# Core dependencies (required for API mode - lightweight)
pillow>=10.0.0
numpy>=1.24.0
huggingface_hub>=0.20.0

# Optional: Local model support (heavy - only install if using --local flag)
# Uncomment these lines if you want to run models locally on CPU:
torch>=2.0.0
torchvision>=0.15.0
diffusers>=0.21.0
transformers>=4.30.0
accelerate>=0.20.0

# Development tools (optional)
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
